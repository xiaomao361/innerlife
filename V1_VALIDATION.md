# InnerLife v1 验证记录

日期：2026-06-22

## 已验证

- 自动测试：29 项全部通过。
- 固定边界场景：9 项全部通过。
- MCP stdio 真连接：通过，9 个工具可发现并调用。
- HTTP API：状态和 Agent 简报通过。
- 后台单次循环：能写入心跳并自动处理排队内容。
- 后台真实模型闭环：自动消费排队内容，写入运行结果，无错误。
- 正式后台：已切换线上 DeepSeek，后台服务正在运行。
- MCP 服务真连接通过，共 12 个工具。
- Claude Code 已正式挂载为 Clara，session start/end 真调用通过。
- Hermes 已正式挂载为 Lara，session start/end 真调用通过。
- 宿主无关两轮闭环：重要问题能在下一会话接回。
- 宿主无关无余韵闭环：普通查询不会产生内部变化。
- DeepSeek 不规范输出：自动纠正重试，失败时保留会话。
- 并行会话：能识别会话期间的状态变化，避免无感覆盖。
- Memoria 同步：增量导入且重复运行不重复写入。
- Continuity 同步：保持“当前位置而非事实”的边界。
- 真实模型：能形成有来源的新问题；无新材料时保持不变。
- 管理页面：桌面正常渲染，无控制台错误。
- 页面交互：Clara/Lara 切换和手动消化按钮通过。
- 移动端：主要内容和控制项可访问。
- launchd 文件：格式检查通过。
- 管理页面 launchd 服务：已安装并正在运行。
- 后台 launchd 服务：已安装，等待正式模型就绪后启动。
- 重启恢复和数据库备份：通过。

## 真实模型试跑

模型：`deepseek-v4-pro`

第一次输入：

> InnerLife 的日常循环以后可以交给本地小模型，但我还没有想清楚哪些情况应该升级到更强模型。

结果：

- 形成一个有来源的新问题。
- 增加一个未完成问题。
- 没有产生待分享内容。

第二次没有新输入：

- 正确返回没有变化。
- 没有重复制造内部事件。
- 原状态保持不变。

## 没有自动完成的外部配置

没有把现有云端密钥写入项目或开机启动文件。

用户需要在私有文件中选择并配置正式模型：

```text
~/.claracore/innerlife/innerlife.env
```

这是安全边界，不是系统功能缺失。
