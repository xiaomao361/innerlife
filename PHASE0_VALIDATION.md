# InnerLife Phase 0 验证记录

验证日期：2026-06-22

## 结果

- 自动测试：16 项全部通过
- 固定场景：9 项全部通过
- 命令行完整流程：通过
- 进程重启后状态恢复：通过
- 本地兼容接口模拟：通过
- 真实模型首次消化：通过
- 真实模型无新材料再次消化：通过，正确返回“没有变化”

## 真实模型试跑

使用当前环境中的 Anthropic Messages 兼容连接，模型为 `deepseek-v4-pro`。

输入：

> InnerLife 的日常循环以后可以交给本地小模型，但我还没有想清楚哪些情况应该升级到更强模型。

第一次运行：

- 形成一个有明确来源的新问题
- 增加一个未完成问题
- 没有产生待分享内容

第二次运行：

- 没有新输入
- 返回没有变化
- 没有重复制造内部事件
- 原状态保持不变

真实试跑使用临时数据库：

```text
/private/tmp/innerlife-real-smoke/innerlife.db
```

没有写入正式状态。

## 验证中发现并修复

1. `zhouwei` 环境必须使用 `python`，使用 `python3` 会落到系统解释器。
2. 真实模型在“没有变化”时可能把空状态返回为 `null`。现在会安全归一化为空对象。
3. 变化来源现在只允许引用本次真正提供给模型的材料，不能引用数据库中未进入本轮上下文的旧记录。

## 当前明确未做

- 常驻 daemon
- 自动调度
- MCP
- 真实 Memoria 和 Continuity 接入
- 主动推送
- Claravision 联动

这些属于下一阶段，不影响 Phase 0 核心循环成立。
